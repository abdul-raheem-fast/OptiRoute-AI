# Slim runtime dependencies for the Vercel serverless function (api/index.py).
#
# The serving path is pure NumPy/pandas inference over the frozen weights in
# routing/models/router_weights.npz - no LLM API call and no API key required.
# matplotlib/seaborn (in the root requirements.txt) are intentionally omitted:
# only routing/plots.py imports them for figure generation, and the demo server
# never does. uvicorn stays because webapp/server.py imports it at module load.
numpy>=2.4.6
pandas>=3.0.5
fastapi>=0.141.1
uvicorn>=0.52.4
pydantic>=2.13.5
